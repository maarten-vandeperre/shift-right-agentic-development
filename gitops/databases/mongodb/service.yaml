apiVersion: v1
kind: Service
metadata:
  name: mongodb
  namespace: agentic
  labels:
    app: mongodb
spec:
  selector:
    app: mongodb
  ports:
    - name: tcp-mongodb
      port: 27017
      targetPort: 27017
      protocol: TCP
  type: ClusterIP
