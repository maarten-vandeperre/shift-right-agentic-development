apiVersion: v1
kind: Service
metadata:
  name: postgresql
  namespace: agentic
  labels:
    app: postgresql
spec:
  selector:
    app: postgresql
  ports:
    - name: tcp-postgresql
      port: 5432
      targetPort: 5432
      protocol: TCP
  type: ClusterIP
