apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: cdc-service
  namespace: agentic
  labels:
    app: cdc-service
spec:
  to:
    kind: Service
    name: cdc-service
  port:
    targetPort: 8080
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
