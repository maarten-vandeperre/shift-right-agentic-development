apiVersion: v1
kind: Service
metadata:
  name: cdc-service
  namespace: agentic
  labels:
    app: cdc-service
spec:
  selector:
    app: cdc-service
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
  type: ClusterIP
