apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: person-service
  namespace: agentic
  labels:
    app: person-service
spec:
  to:
    kind: Service
    name: person-service
  port:
    targetPort: 8080
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
