apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: chat-service
  namespace: agentic
  labels:
    app: chat-service
spec:
  to:
    kind: Service
    name: chat-service
  port:
    targetPort: 8080
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
