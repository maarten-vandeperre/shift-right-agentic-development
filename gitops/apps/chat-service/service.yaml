apiVersion: v1
kind: Service
metadata:
  name: chat-service
  namespace: agentic
  labels:
    app: chat-service
spec:
  selector:
    app: chat-service
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
  type: ClusterIP
