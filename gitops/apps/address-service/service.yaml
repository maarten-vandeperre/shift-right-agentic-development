apiVersion: v1
kind: Service
metadata:
  name: address-service
  namespace: agentic
  labels:
    app: address-service
spec:
  selector:
    app: address-service
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
  type: ClusterIP
