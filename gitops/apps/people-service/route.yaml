apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: people-service
  namespace: agentic
  labels:
    app: people-service
spec:
  to:
    kind: Service
    name: people-service
  port:
    targetPort: 8080
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
