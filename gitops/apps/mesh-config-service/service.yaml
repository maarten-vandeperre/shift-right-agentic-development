apiVersion: v1
kind: Service
metadata:
  name: mesh-config-service
  namespace: agentic
  labels:
    app: mesh-config-service
spec:
  selector:
    app: mesh-config-service
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
  type: ClusterIP
